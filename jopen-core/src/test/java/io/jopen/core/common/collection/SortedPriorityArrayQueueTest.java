package io.jopen.core.common.collection;

import org.junit.Test;

/**
 * @author maxuefeng
 */
public class SortedPriorityArrayQueueTest {

    @Test
    public void testNewSortedArrayQueue() {

        // 转换异常：java.lang.ClassCastException: [Ljava.lang.Object; cannot be cast to [Lio.jopen.core.common.collection.Queue$Node;
        // SortedArrayQueue<Integer> arrayQueue = new SortedArrayQueue<>();

        // 转换异常：java.lang.ClassCastException: [Ljava.lang.Object; cannot be cast to [Lio.jopen.core.common.collection.Queue$Node;
        // SortedArrayQueue<Integer> arrayQueue = new SortedArrayQueue<>(Integer.class);
    }
}
